[package]
name = "central_processor"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.36", features = ["full"] }
bytes = "1.5"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
# linfa = "0.7" # For actual ML, you would include a Rust ML framework here
